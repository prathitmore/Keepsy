rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow anyone to read/write for now to verify the connection
    // We will tighten this once we confirm the 403 is gone
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
